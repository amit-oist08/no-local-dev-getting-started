<div class="container">

  <table>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Email</th>
      <th> New Value </th>
    </tr>

    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.id %></td>
        <td><%= contact.name %></td>
        <td><%= contact.email %></td>
        <td><%= text_field "contact.name" %></td>
      </tr>
    <% end %>
  </table>

</div>
